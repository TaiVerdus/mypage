# 部署到公网：让任何人都能和数字分身聊

> **目标**：访客打开网页就能聊，**不需要你的电脑开着**。
> **做法**：页面 + 一层薄代理一起部署。代理保管 API key，并在服务端锁死分身的人设。

---

## 0. 为什么不能让页面直接调 DeepSeek

页面是**纯静态公开**的。任何写进 `script.js` 的 key，按 F12 或看一眼网络请求就抄走了，
然后拿你的余额当自己的用。而且 **CORS 挡不住** —— 它只是浏览器的规矩，`curl` 根本不理会。

所以 key 必须待在服务端：

```
访客浏览器 ──POST /api/chat──▶ 你的代理（server.js）
                                 · 从环境变量读 key（浏览器永远看不到）
                                 · 丢掉客户端传来的 system，注入分身人设
                                 · 限速 + 限制输出长度 + 限制历史条数
                                ──▶ https://api.deepseek.com/chat/completions
```

这一层还顺手挡住一件事：**有人拿到地址直接打，也只能跟「王释贤的数字分身」说话**，
不能拿你的额度当免费 ChatGPT 用（有回归测试盯着，见 `tools/test-chat-proxy.js` 第 3 / 8 节）。

---

## 1. 拿一个 DeepSeek API key

1. 打开 <https://platform.deepseek.com> 注册
2. **API keys** → **Create new API key** → **立刻复制**（关掉页面就再也看不到了）
3. **Billing** 里充一点：**充 ¥10 就够很久** —— 按本页面的用量（每轮约 500 输入 + 200 输出），
   一次对话约 **$0.0002**，1000 次约 **¥1.5**

### ⚠️ 模型名别照抄网上的教程

官方文档（2026-09-23 实测）：

| | |
| --- | --- |
| 现在的模型 | **`deepseek-flash`** |
| `deepseek-chat` / `deepseek-reasoner` | **官方文档里已经没有这两个名字了**（网上大量教程仍是旧的） |
| 端点 | `https://api.deepseek.com`（OpenAI 兼容） |

**价格有峰谷**：高峰只有**周一至周五 09:00–12:00 与 14:00–18:00（北京时间）**；
其余时间（含晚上、周末、法定节假日）**全部半价**。被人晚上访问反而更便宜。

---

## 2. 本机先验一遍

```bash
cd "D:\Project-Based_CST_AI_Foundations\Vibe Coding\MYPAGE-V2.0"
DEEPSEEK_API_KEY=sk-你的一串 node server.js      # 默认 8080 端口
# 打开 http://localhost:8080/  问一句
```

⚠️ **本机打开时页面会优先用本机 ollama**（如果它在跑）。想强制走云端代理：

```
http://localhost:8080/?chat=/api/chat
```

想不在真 key 上花钱先验链路，可以指到一个假端点（`tools/openai-stub.py`）：

```bash
DEEPSEEK_BASE=http://127.0.0.1:8005 DEEPSEEK_API_KEY=test node server.js
```

---

## 3. 部署

`server.js` 是**零依赖的 Node 服务**（只用 Node 内置模块），任何能跑 Node 的地方都能跑。
**唯一必配的环境变量是 `DEEPSEEK_API_KEY`**，其余都有合理默认。

> ⏳ **托管选哪家还没定**（这决定链接长什么样、以及国内访客的可达性）。
> 候选见 `PROJECT.md` 的「未结项」。定下来后这一节补上具体命令。

---

## 4. 上线前的检查清单

- [ ] **隐私扫一遍**：页面里不该出现住址、电话、具体年龄、学号、以及任何别人没同意公开的信息
      （项目此前做过一轮，见 `PROJECT.md` 的隐私扫描记录；改了内容就要重扫）
- [ ] **联系方式想清楚**：邮箱和 QQ 号公开在页面上是能被爬的。介意的话只留邮箱
- [ ] **`/healthz` 探一下**：应该返回 `keyConfigured: true`
- [ ] **代理不要暴露源码**：`server.js` 已经默认把 `/tools/`、`/data/`、`/PROJECT.md`、
      `/server.js` 这些挡成 404（有回归测试）
- [ ] **在手机上开一次**：窄屏布局 + 「不要双击 index.html」那条坑
- [ ] **`og:image` / `og:url` 换成真实绝对网址**（分享到微信时才有缩略图；现在是 TODO 注释）
- [ ] **知识库里还有 3 个快捷按钮点了只说「我不知道」** —— 上线前最好补掉（内容要你自己给）

---

## 5. 环境变量一览（都有默认值，只有第一个必填）

| 变量 | 默认 | 作用 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | — | **必填** |
| `DEEPSEEK_BASE` | `https://api.deepseek.com` | 换它就能指到别家兼容端点 |
| `CHAT_MODEL` | `deepseek-flash` | 模型名 |
| `PORT` | `8080` | 端口 |
| `MAX_OUTPUT_TOKENS` | `400` | 单次回答长度上限（＝限制有人拿它写长文烧额度） |
| `MAX_HISTORY_MSGS` | `12` | 只带最近几条历史，防止越聊越贵 |
| `MAX_INPUT_CHARS` | `600` | 单条提问长度上限 |
| `RATE_PER_MIN` | `8` | 每个 IP 每分钟最多问几次 |
| `ALLOW_ORIGIN` | `*` | 跨域放行（同源部署时用不到） |

---

## 6. 页面侧现在是三条路，而且是「依次降级」（不用你改，说明一下行为）

```
① 显式 url（?chat= 临时覆盖 / 将来的 WeClone 地址）        ← 最优先
② 本机 ollama（只有 localhost / 127.0.0.1 / file:// 打开时）← 开发用，免费又快
③ cloudUrl = /api/chat（部署后的代理）                     ← 公网访客走这条
        ↓ 以上任何一个不通，自动试下一个
    本地知识库（永远可用，页面永不是坏的）
```

⚠️ ② 排在 ③ 前面是**故意的**：你自己开发时用本地模型，公网访客才走云端。
⚠️ 但它们是**依次尝试**的，不是"挑一个就走" —— 这是 2026-09-23 修的一个真坑：
以前"第一个失败就直接落回知识库"，导致**双击 `index.html`** 打开时
（`file://` 下 ollama 必然 403）明明可用的云端**永远轮不到**，看起来就是「没接上」。
⚠️ 不管走哪条，**公网解析出来的地址里绝不会出现本机地址** —— 有专门的回归测试守着
（`tools/test-chat-backend.js` 第 9.3 与第 11.4 节）。
⚠️ 代价：极端情况下会多等一轮；但本地服务失败通常是**即时**的（403 / 连接拒绝），不会真等满超时。

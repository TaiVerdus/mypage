# 部署到公网：让任何人都能和数字分身聊

> **目标**：访客打开网页就能聊，**不需要你的电脑开着**。
> **做法**：页面 + 一层薄代理一起部署。代理保管 API key，并在服务端锁死分身的人设。

---

## 0. 为什么不能让页面直接调 DeepSeek

页面是**纯静态公开**的。任何写进 `script.js` 的 key，按 F12 或看一眼网络请求就抄走了，
然后拿你的余额当自己的用。而且 **CORS 挡不住** —— 它只是浏览器的规矩，`curl` 根本不理会。

所以 key 必须待在服务端：

```
访客浏览器 ──POST /api/chat──▶ 你的代理（server.js）
                                 · 从环境变量读 key（浏览器永远看不到）
                                 · 丢掉客户端传来的 system，注入分身人设
                                 · 限速 + 限制输出长度 + 限制历史条数
                                ──▶ https://api.deepseek.com/chat/completions
```

这一层还顺手挡住一件事：**有人拿到地址直接打，也只能跟「王释贤的数字分身」说话**，
不能拿你的额度当免费 ChatGPT 用（有回归测试盯着，见 `tools/test-chat-proxy.js` 第 3 / 8 节）。

---

## 1. 拿一个 DeepSeek API key

1. 打开 <https://platform.deepseek.com> 注册
2. **API keys** → **Create new API key** → **立刻复制**（关掉页面就再也看不到了）
3. **Billing** 里充一点：**充 ¥10 就够很久** —— 按本页面的用量（每轮约 500 输入 + 200 输出），
   一次对话约 **$0.0002**，1000 次约 **¥1.5**

### ⚠️ 模型名别照抄网上的教程

官方文档（2026-09-23 实测）：

| | |
| --- | --- |
| 现在的模型 | **`deepseek-flash`** |
| `deepseek-chat` / `deepseek-reasoner` | **官方文档里已经没有这两个名字了**（网上大量教程仍是旧的） |
| 端点 | `https://api.deepseek.com`（OpenAI 兼容） |

**价格有峰谷**：高峰只有**周一至周五 09:00–12:00 与 14:00–18:00（北京时间）**；
其余时间（含晚上、周末、法定节假日）**全部半价**。被人晚上访问反而更便宜。

---

## 2. 把 key 放进 `.env`（唯一必配的东西）

项目根目录建一个 `.env`：

```
DEEPSEEK_API_KEY=sk-你的一串
```

⚠️ **为什么是文件、不是环境变量**：线上发布沙箱**没有配置环境变量的入口**，
key 只能随项目一起上传 ⇒ `server.js` 启动时读这个文件（见那边的 `loadDotEnv`）。

⚠️ **本机的环境变量依然优先**（`DEEPSEEK_API_KEY=xx node server.js` 不会被文件盖掉）。

⚠️ 建完**先确认它没进 git**：

```bash
git check-ignore .env      # 要有输出（说明被忽略）。没有输出就先别往下做
```

## 3. 本机先验一遍，然后发布

**先在本机跑通**（不花部署的时间）：

```bash
node server.js                # 默认 8080 端口
# 打开 http://localhost:8080/ 问一句
```

- 本机打开时页面**默认优先用本机 ollama**；想强制走云端：`http://localhost:8080/?chat=/api/chat`
- 想**不花真钱**先验链路：`DEEPSEEK_BASE=http://127.0.0.1:8005 node server.js`
  （配 `tools/openai-stub.py` 那个假端点）

**发布**用内置的「发布为应用」能力（我可以直接做，你说一声就行）。它会：
**上传源码 → 在沙箱里装依赖 → 起服务 → 返回一个分享链接**。

项目已经满足它对项目的要求：服务监听 `PORT` 环境变量、绑 `0.0.0.0`、零依赖、
`package.json` 里有 `start` 脚本。

### ⚠️ 发布前必查（这两条漏一条就会出事）

| 检查 | 为什么 |
| --- | --- |
| `git check-ignore .env` **有输出** | 仓库是公开的。key 一旦被提交就等于公开，别人能拿它烧你的额度 |
| 线上 `/.env` 必须返回 **404** | 静态托管默认把目录里的文件**原样发出去**。不挡的话，**任何人访问一次就拿到 key** —— 这个洞本地完全看不见（你本来就知道自己的 key），**上线当天就会被扫到** |

`server.js` 已经把**所有「点开头」的路径**都挡成 404（`.env` / `.env.local` / `.gitignore` /
`.git/config` …），并且有回归测试盯着（`tools/test-chat-proxy.js` 第 2 节）。

## 4. 上线前的检查清单

- [ ] **隐私扫一遍**：页面里不该出现住址、电话、具体年龄、学号、以及任何别人没同意公开的信息
      （项目此前做过一轮，见 `PROJECT.md` 的隐私扫描记录；改了内容就要重扫）
- [ ] **联系方式想清楚**：邮箱和 QQ 号公开在页面上是能被爬的。介意的话只留邮箱
- [ ] **`/healthz` 探一下**：应该返回 `keyConfigured: true`
- [ ] **代理不要暴露源码**：`server.js` 已经默认把 `/tools/`、`/data/`、`/PROJECT.md`、
      `/server.js` 这些挡成 404（有回归测试）
- [ ] **在手机上开一次**：窄屏布局 + 「不要双击 index.html」那条坑
- [ ] **`og:image` / `og:url` 换成真实绝对网址**（分享到微信时才有缩略图；现在是 TODO 注释）
- [ ] **分身的答案是否与事实档一致**：`python tools/build-persona.py --check` 应为 0 处需要更新
- [ ] **要不要再补问答**：知识库现有 **12 条**（2026-09-23 用户补了一批：专业 / 课程 / 性格 / 联系 / Jarvis / 主页）。
      以后想让它多知道什么，改 `data/persona.json` 的 `facts` / `about` 再跑生成器；
      测试里 `KNOWN_GAP` 那一栏就是「访客会问、它答不上」的标志位

---

## 5. 环境变量一览（都有默认值，只有第一个必填）

| 变量 | 默认 | 作用 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | — | **必填** |
| `DEEPSEEK_BASE` | `https://api.deepseek.com` | 换它就能指到别家兼容端点 |
| `CHAT_MODEL` | `deepseek-flash` | 模型名 |
| `PORT` | `8080` | 端口 |
| `MAX_OUTPUT_TOKENS` | `400` | 单次回答长度上限（＝限制有人拿它写长文烧额度） |
| `MAX_HISTORY_MSGS` | `12` | 只带最近几条历史，防止越聊越贵 |
| `MAX_INPUT_CHARS` | `600` | 单条提问长度上限 |
| `RATE_PER_MIN` | `8` | 每个 IP 每分钟最多问几次 |
| `ALLOW_ORIGIN` | `*` | 跨域放行（同源部署时用不到） |
| `THINKING` | `disabled` | 思考模式开关。**默认关**，理由见下面那节 |
| `THINKING_EFFORT` | — | 仅 `THINKING=enabled` 时有意义：`low` / `high` / `max` |
| `BALANCE_MIN` | `1` | 余额低于这个数就**暂时不接活**（页面自动落回知识库） |
| `BALANCE_TTL_MS` | `900000` | 余额多久重查一次（15 分钟）。**查不到不拦** |

### 为什么默认关掉 thinking（2026-09-23 实测定下来的）

`deepseek-flash` **默认开启思考模式、档位 `high`**（官方文档）。对「闲聊型数字分身」它有三处不划算，都是实测或文档来的：

1. **思考过程占的是同一份 `max_tokens` 预算** —— 实测：少了人设时会一口气写 660 字思考，
   输出**正好撞上 400 上限**、回答被截断。带人设时思考只有 47 字（不撞），但风险一直在。
2. **输出 token 75 → 48**（约省 35%）—— 输出按 token 计费，而输出比输入贵 4 倍。
3. ⚠️ **thinking 模式下 `temperature` 是被官方忽略的**（文档明写）。
   而分身这种闲聊恰恰需要一点温度差 —— 关掉之后 `temperature: 0.8` 才真正生效。

想改回：`THINKING=enabled`（可再配 `THINKING_EFFORT=low` 走降档）。

### 余额护栏（为什么需要它）

**DeepSeek 的 key 不能设额度上限** —— 能兜住的只有「按 IP 限速」与「余额本身」。
所以余额低于 `BALANCE_MIN` 时代理干脆返回 503，让页面**自动落回本地知识库**
（而不是等上游报错、转圈）。403/503 这些在页面上都表现为「离线版回答」，访客不会看到白屏。

⚠️ **查不到余额时**（网络抖、接口抽风）**不拦** —— 这是刻意的：宁可晚一点降级，
也不该让一个临时故障把分身停摆。
⚠️ `/healthz` 里能看到余额与思考模式的现状（**只读、不含 key**）。

---

## 6. 页面侧现在是三条路，而且是「依次降级」（不用你改，说明一下行为）

```
① 显式 url（?chat= 临时覆盖 / 将来的 WeClone 地址）        ← 最优先
② 本机 ollama（只有 localhost / 127.0.0.1 / file:// 打开时）← 开发用，免费又快
③ cloudUrl = /api/chat（部署后的代理）                     ← 公网访客走这条
        ↓ 以上任何一个不通，自动试下一个
    本地知识库（永远可用，页面永不是坏的）
```

⚠️ ② 排在 ③ 前面是**故意的**：你自己开发时用本地模型，公网访客才走云端。
⚠️ 但它们是**依次尝试**的，不是"挑一个就走" —— 这是 2026-09-23 修的一个真坑：
以前"第一个失败就直接落回知识库"，导致**双击 `index.html`** 打开时
（`file://` 下 ollama 必然 403）明明可用的云端**永远轮不到**，看起来就是「没接上」。
⚠️ 不管走哪条，**公网解析出来的地址里绝不会出现本机地址** —— 有专门的回归测试守着
（`tools/test-chat-backend.js` 第 9.3 与第 11.4 节）。
⚠️ 代价：极端情况下会多等一轮；但本地服务失败通常是**即时**的（403 / 连接拒绝），不会真等满超时。

## 7. 流式与运维观测（2026-09-23 加）

**流式**：页面要流式时（请求体里 `{"stream": true}`），代理把上游的 SSE **原样透传**；
上游若不回 `event-stream`（有的兼容端点会忽略这个参数），就按整包 JSON 返回 ——
页面按 `content-type` 分流，两种都能读，所以**换上游不会把这层弄坏**。

⚠️ 中途断开时，**已经流出去的部分不会收回**（代理只结束响应）—— 页面对此的处理是：
把已收到的字留在屏上；如果一个候选都没成，才撤掉那半句、落回知识库。

实测（真 DeepSeek、带人设、关着 thinking）：首字 **636 ms**、整段 **832 ms**、`finish_reason=stop`，
增量分 5 段以上到达 —— 也就是说访客不再需要干等整段答完。

**运行计数**：`/healthz` 多了一个 `stats`（只读、不含 key、不含访客内容）：

| 字段 | 涨起来说明什么 |
| --- | --- |
| `chat` | 真正打到上游（**会花钱**）的次数 |
| `streams` | 其中走了流式的次数 |
| `rateLimited` | 被按 IP 限速拦下的次数 ⇒ 有人在刷，或你自己在反复刷新 |
| `upstreamErrors` | 上游报错 / 超时 / 流中断的次数 ⇒ 去看日志里的 `[chat]` 那几行 |
| `balanceBlocks` | 余额护栏拦下的次数 ⇒ 该充值了 |
| `uptimeSec` | 这一进程活了多久 |

⚠️ **限速器与这套计数都只在进程内存里**：重启清零、多实例不共享 ——
单实例部署完全够用（个人主页就是这个量级）。**不要为它改架构**；
真要历史趋势，那是接外部监控的事，跟这个项目无关。

# 提问记录：大家最常问分身什么

> 2026-09-26 建立。用户要求：「记录一下大家问 AI 分身什么问题比较多，整理一下跟我说，再更新大模型记忆」。

## 1. 记什么、不记什么

| | 内容 |
| --- | --- |
| **记** | 每一句向数字分身的**提问文字**（最多 500 字）+ 数据库自己写的时间 |
| **不记** | 访客身份（没有 IP、没有设备、没有昵称）、分身的**回答**、多轮对话的上下文、访客在页面上填过的任何东西 |
| 存在哪 | 云服务的 `chat_questions` 表 |
| 谁能读 | **只有我**（访客读不到：anon 只被授予 INSERT，匿名读取会被挡回 401） |

**为什么只记问题、不记回答**：统计「大家关心什么」只需要问题；回答是分身的产出，存起来没有分析价值，却会成倍放大隐私面。

## 2. 数据流

```
访客在页面提问
   └─ script.js 的 ask() 先匿名记一条（fire-and-forget，失败静默）
        └─ POST {endpoint}/.cloud/database/rest/chat_questions
             └─ chat_questions 表（只能写、读不了）
   └─ 然后才是正常流程：云端模型 或 本地知识库兜底
```

**为什么挂在页面而不是后端**（这是个刻意的选择）：后端只看得见「走云端」的提问 ——
余额护栏触发、断网、纯离线兜底的那些提问它**根本收不到**，而那些同样是真实提问。
挂在 `ask()` 里就一次覆盖全部路径。

**清楚它的边界**（别当成 bug）：

- **镜像站（`taiverdus.github.io`）上不记**：平台跨域只放行「正式域名 / 本机 / `file://`」，
  镜像站被 403 拦住 —— 与反馈表单同一个限制；
- **被浏览器插件拦截时不记**：静默失败，不影响聊天；
- **重新发布之前的提问不可能有**：这张表 2026-09-26 才建，历史无法回溯。

## 3. 表结构与权限

```
chat_questions
  id          bigint  主键（自增）
  question    text    not null，1~500 字（CHECK 约束）
  created_at  timestamptz  默认 now()  ← 时间由数据库写，不信任客户端
```

权限（两道门，缺一不可）：

1. **GRANT**：只给 `anon` / `authenticated` 的 `INSERT`。平台建表时会默认授一堆权限（SELECT / UPDATE / DELETE / TRUNCATE / …），已全部 REVOKE —— 这叫**纵深防御**：就算哪天 RLS 被误关，访客仍然读不到；
2. **RLS**：已开启，且只有一条 `INSERT` 策略 ⇒ 读 / 改 / 删一律拒绝。
   实测：匿名带公开密钥查询，返回 **401 permission denied**（不是空数组）。

## 4. 怎么读这些数据（两条路）

- **自己看**：云服务面板 → 数据管理 → `chat_questions` 表；
- **让我整理**：说一句「整理一下提问统计」，我会查表 + 归类，给你一张
  「高频问题 / 值得回流到人设的问题」清单。想定期自动出，也可以做成每周一次的自动化任务。

## 5. 「更新大模型记忆」怎么做（FAQ 回流）

统计本身不改分身的行为。要让分身**因为大家常问而答得更好**，走这条链：

```
① 看统计 ⇒ ② 挑出「高频 + 分身答得不好/没料」的问题
   ⇒ ③ 写进 data/persona.json（facts 补事实 / kb 补离线答案）
      ⇒ ④ 跑 tools/build-persona.py（+ --check）
         ⇒ 两份人设（script.js 本机用 / server.js 公网用）+ 离线知识库**同源重生**
            ⇒ ⑤ 重新发布，线上才生效
```

三条必须记住的：

- **事实只改 `data/persona.json`** —— 人设是生成物，手改两个文件必然漂移（`--check` 会当场报出来）；
- **拿不准的事不要写进人设**：分身宁可说不知道，也不能编（这条是它最值钱的特性）；
- **改人设 = 改服务端** ⇒ **必须重新发布**（每日推荐那种数据不用，人设不行）。

## 6. 维护命令

```bash
# 页面真身代码的回归（抽 script.js 里的函数原文在 node 里跑，验证请求形状）
node tools/test-question-log.js
# 跑完清理它写的那条：
#   DELETE FROM chat_questions WHERE question LIKE 'TESTQLOG%';

# 统计（在对话里说「整理一下提问统计」即可，我这边直接查表）
```

## 7. 顺带

页脚那句隐私说明是**如实**的：它写了「向分身的提问也会匿名记一条，用来统计大家最关心什么
（不记身份、不记回答）」。**改了记录行为，就一定要回头改这句** —— 页面上不留假话。

# MYPAGE 设计系统 v1.0

> 规范来源：规范范（DesignMdArchitect）。本文件是 MYPAGE 的**唯一视觉真相源**。
> 改 `style.css` 前先看这里；新增样式必须能用下面的 token 表达，不允许再写裸色值。

---

## 1. 品牌定位与调性

| 项 | 定调 |
| --- | --- |
| 一句话 | 「冷静的科技感 + 有体温的个人感」 |
| 关键词 | 活跃 · 克制 · 通透 · 可信赖 |
| 受众 | 先是同学朋友，后是 HR / 面试官 |
| 不该像 | 花哨的模板站、糖果色、纯黑白极简（太冷） |

青色是主色但不是全部：**青色只用于强调和交互**，大面积留白交给浅底与白色卡片。

---

## 2. 设计原则

1. **一个值只定义一次** — 任何色值、间距、圆角都走 token，禁止裸写 `#xxx`。
2. **留白先于装饰** — 想强调一块内容，先加间距，其次才是颜色和动效。
3. **动效服务于理解** — 动画只解释"从哪来、到哪去"；默认尊重 `prefers-reduced-motion`。
4. **移动端优先** — 先保证 375px 可用，再往上加布局。

---

## 3. 色彩系统

### 3.1 基础色（**全站只有 6 个**）

> V2.3 从 15 色收敛到 6 色。灵感来自 serious.business：它全站只有 2 个颜色、
> 0 个深色区块，照样不平。**颜色越少，层次反而越清楚。**

| Token | 值 | 用途 |
| --- | --- | --- |
| `--c-ink` | `#0F172A` | 主文字 |
| `--c-muted` | `#475569` | 次要文字 |
| `--c-brand` | `#0F766E` | 青 · **全站唯一强调色** |
| `--c-mint` | `#CCFBF1` | 浅青 · 标签底 / 光晕 |
| `--c-bg` | `#F0FDFA` | 页面底 |
| `--c-surface` | `#FFFFFF` | 卡片 |

**纪律**：想加新颜色前，先问能不能用这 6 个 + 透明度表达出来。

### 3.1.1 深色主题（V2.7 续二十一，2026-09-18）

> **触发**：用户要求「背景做成黑色 + 光标效果做成彩色，像 CodePen Shape Wave 那样」。
> **一个必须认识到的连带后果**：画布铺黑 = 全站底色变黑，深墨正文在纯黑上会整个看不见 ——
> 所以这不是「改个背景色」，而是**换主题**。

开关：`<html data-theme="dark">`。**删掉这一个属性就回到浅色**，两套方案都完整留在 `style.css` 里。

| Token | 浅色（原） | 深色（现） | 为什么 |
| --- | --- | --- | --- |
| `--c-bg` | `#F0FDFA` | `#000000` | 用户指定纯黑 |
| `--c-surface` | `#FFFFFF` | `#0B1211` | 比页面底略亮 + 带一点青，卡片才有层次 |
| `--c-ink` | `#0F172A` | `#F1F5F9` | 主文字反相 |
| `--c-muted` | `#475569` | `#9FB0C0` | 次要文字 |
| `--c-brand` | `#0F766E` | `#5EEAD4` | ⚠️ **强调色必须变亮** |
| `--c-mint` | `#CCFBF1` | `#10302C` | 它在浅色里是「标签底」，深色里要反过来 |
| `--c-brand-rgb` | `15 118 110` | `94 234 212` | ⚠️ **派生通道也要一起换** |

**两条深色主题的常识**（这次都踩到了）：

1. **强调色不变亮 = 不达标**：青 `#0F766E` 在纯黑上只有 **3.3:1**，够不着 AA 的 4.5:1；
   换成浅青 `#5EEAD4` 后是 **14.2:1**
2. **派生通道必须一起换**：`--c-line` / `--sh-*` 都是从 `--c-brand-rgb` 派生的，
   通道值不换，描边和阴影在深底上等于不存在

**实测对比度**（全部 ≥ WCAG AA）：主文字 19.2:1 / 次文字 9.5:1 / 强调色 14.2:1 /
渐变暗端 8.4:1 / 选中态 9.6:1。浅色主题未动，复查仍是 17.1 / 7.3 / 5.25:1。

**需要深色主题单独交代的硬编码浅色面，现在只剩一处**：`.chat-avatar`。
其余全部走语义 token —— 所以换主题只改 token 就够，业务代码一行没动。

> ⚠️ 更新（2026-09-22，V2.7 续四十七 / 续四十八）：
> ① `.chat-avatar` 里那层半透明白底现在**只是图片的加载占位** —— 那一格放的是头像照片
>    （`images/avatar.jpg`），不是文字。深色主题里那条覆写（`rgba(255,255,255,.10)`）
>    **照旧保留**，否则头像闪出来之前会露出聊天窗标题的青绿渐变。
> ② `.hero-eyebrow`（「CST 与 AI 基础 · 2026」胶囊）**已随首屏眉题一起删除**，
>    它原来那处白色面（`rgba(255,255,255,.6)`）连同深色主题的覆写都清掉了。
> ③ 另有一层不在此列的白色底：`.photo-no` / `.photo-credit`（照片编号与署名胶囊，
>    `rgba(255,255,255,.92)`）—— 它们**压在照片上**，浅色/深色主题下观感一致，
>    所以从来不需要覆写，**不算「要单独交代」的那一类**。

### 3.1.2 效果层不受配色纪律约束（V2.7 续二十）

形状场画布（`#fxField`）用的是 pen 原版的 **10 色 + 7 组渐变**彩虹色板。
这是**用户明确要求**的（「光标效果做成彩色」），属于**效果层**，不是界面配色：
界面仍然只有一套语义色，彩色只出现在「**不承载信息的装饰层**」上。

> **记录在案**：这条纪律是**被改掉的，不是被忽略的**。
> 想恢复「效果层也用 6 色」，把 `script.js` 里 `FX_COLORS` 换回青色那四级即可。

### 3.2 派生层次（不新增颜色，只改透明度）

通道：`--c-brand-rgb: 15 118 110`、`--c-bg-rgb: 240 253 250`

| Token | 值 | 用途 |
| --- | --- | --- |
| `--c-line` | `rgb(var(--c-brand-rgb) / .14)` | 默认描边 / 分隔线 |
| `--c-line-strong` | `rgb(var(--c-brand-rgb) / .30)` | 强调分隔线 |
| `--c-brand-deep` | `color-mix(in srgb, brand 82%, ink)` | 渐变**暗端**（兜底 `#0F6562`） |

> 关键改动：渐变原来从主色走向浅青 `#14B8A6`，白字压在亮端对比度只有 **2.5:1**。
> 现在改为走向**更深的**青，对比度 **6.9:1**。渐变要往暗里走，不要往亮里走。

### 3.3 语义映射（**业务代码只许用这些**）

| 语义 token | 指向 |
| --- | --- |
| `--color-ink` | `--c-ink` |
| `--color-muted` | `--c-muted` |
| `--color-brand` | `--c-brand` |
| `--color-brand-deep` | `--c-brand-deep` |
| `--color-mint` | `--c-mint` |
| `--color-bg` | `--c-bg` |
| `--color-surface` | `--c-surface` |
| `--color-line` | `--c-line` |
| `--color-line-strong` | `--c-line-strong` |

### 3.4 对比度校验（WCAG AA）

| 组合 | 比值 | 结论 |
| --- | --- | --- |
| `--c-ink` on `--c-bg` | ≈ 17.5:1 | ✅ |
| `--c-muted` on `--c-bg` | ≈ 7.3:1 | ✅ |
| `--c-brand` on `--c-surface` | ≈ 5.5:1 | ✅ |
| `--c-brand` on `--c-mint`（标签） | ≈ 4.9:1 | ✅ |
| 白字 on `--c-brand-deep`（按钮渐变暗端） | ≈ 6.9:1 | ✅（原亮端 2.5:1 ❌，已修） |
| marquee 镂空描边字 | — | 纯装饰，已加 `aria-hidden`，不计入 ✅ |

### 3.5 动效：入场颗粒度

整块淡入（`.reveal`）只用于卡片、聊天窗这类**容器**；
**文字一律走逐行入场**（`data-lines`）：JS 按渲染后的实际换行拆行，每行套一层
`overflow: hidden` 遮罩，行内容从遮罩下方升起，行间错开 88ms。
比整块淡入细一档，是「精致感」最便宜的来源。

---

## 4. 字体尺度

| Token | 值 | 场景 |
| --- | --- | --- |
| `--fs-display` | `clamp(40px, 7vw, 68px)` | 首屏 H1 |
| `--fs-hero2` | `clamp(32px, 5.5vw, 48px)` | 联系区大标题 |
| `--fs-h2` | `clamp(26px, 3.6vw, 34px)` | 区块标题 |
| `--fs-h3` | `17px` | 卡片标题 |
| `--fs-body` | `15px` | 正文 |
| `--fs-sm` | `14px` | 卡片内容 |
| `--fs-xs` | `13px` | 说明文字 |
| `--fs-caption` | `12px` | 页脚、角标、**导航栏那句引语** |
| `--fs-eyebrow` | `12px` | 眉题 / 卡片小标签（`.eyebrow`、`.card-row dt`）|

行高：`--lh-tight: 1.18`（标题）、`--lh-body: 1.7`（正文）。

**字重**（2026-09-22 对着样式表实际数了 39 处）：

| 档 | 用在哪 |
| --- | --- |
| `400` | 正文：`body`、首屏自我介绍、导航栏引语 |
| `500` | 标签与小字：`.eyebrow`、`.meta-k`、`.tag-list li`、`.item-idx`、`.item-badge`、`.card-row dt` |
| `600` | `.btn`、卡片小标题 `h3`、`.meta-v`、`.bento-item strong` |
| `700` | 大标题：`h1`、`.section-title`、`.contact-title`、`.nav-logo`、小票标题 |
| `800` | **全站只有一处**：Marquee 的镂空描边字 |

> ⚠️ **同一个选择器可能被后面的规则覆写**：`.contact-title` 先写 500、后面又改成 700；
> `.meta-k` 先写 500、后面被那条分组规则改成 600。
> ⇒ 判断某处字重时**以文件里靠后的那次为准**，别只看到前面那条就下结论。
> ⚠️ 这条以前写的是「只用两档：400 / 600」，**是错的**（500 才是用得最多的一档，共 17 处）。

**小字号（≤12px）的颜色**：只能配 `--color-muted` 或 `--color-brand`，两套主题都过 WCAG AA。
实测对比度：次要灰 **浅色 7.27:1 / 深色 9.45:1**；强调青 **浅色 5.47:1 / 深色 12.80:1**。
⚠️ **不要用 `opacity` 做淡化** —— 跨主题会掉到门槛以下，且 `--c-600` / `--c-700` 那两个 token **早就没有了**（V2.3 收敛掉了）。

---

## 5. 空间与形状

**间距（4px 基准）**：`--sp-1:4` `--sp-2:8` `--sp-3:12` `--sp-4:16` `--sp-5:24` `--sp-6:32` `--sp-7:48` `--sp-8:64`

**圆角**：`--r-sm:10px`（输入框/小按钮）、`--r-md:14px`（卡片/图片）、`--r-lg:18px`（聊天窗/玻璃名片）、`--r-pill:999px`（按钮/标签）

**阴影（三级，不要自造）**

| Token | 值 | 场景 |
| --- | --- | --- |
| `--sh-1` | 三层：`0 1px 2px` + `0 2px 8px` + `0 8px 24px`（`--c-brand-rgb` 的 4~5%）| 静止态卡片 |
| `--sh-2` | 三层：`0 2px 4px` + `0 10px 24px` + `0 24px 48px`（6~10%）| hover 抬起 |
| `--sh-3` | 一层：`0 4px 20px`（10%）| 导航条投影 |

⚠️ 两条意思：① **三级就够，别自造第四级**；② 阴影颜色**一律从 `--c-brand-rgb` 派生**、不用黑灰
—— 换主题时阴影颜色自动跟着走（这也是两套主题只改 token 就够的原因之一）。
⚠️ `--sh-1` / `--sh-2` 是**三层叠出来的**，不是单层：单层只能表达「一个高度」，叠起来才有远近。
这两行以前写成两个具体 rgba 值，**是过时的**。

**动效**

| Token | 值 |
| --- | --- |
| `--dur-fast` | `150ms` |
| `--dur-base` | `220ms` |
| `--dur-slow` | `500ms` |
| `--ease-out` | `cubic-bezier(0.22, 0.61, 0.36, 1)` |

**抬起位移**（hover 的 `translateY`）：**卡片 `-3px`**（`.info-card` / `.photo-item`）、
**按钮与链接 `-2px`**、极小元素 `-1px`；不要出现 `-4px`。
⚠️ 这条以前写「统一 `-2px`」，**与代码不符** —— 实际是按元素体量分三档。
要么把两处卡片改回 `-2px`、要么认下这条新规矩，**待定**（属改动效，没擅自改）。

---

## 6. 组件规范

### 6.1 按钮 `.btn`

| 变体 | 底色 | 文字 | 用法 |
| --- | --- | --- | --- |
| `.btn--primary` | `--color-brand` → `--color-brand-deep` 135° 渐变 | `--color-surface` | **唯一**主行动（首屏提问 / 发送） |
| `.btn--ghost` | `--color-surface`（＋一层淡青投影）| `--color-brand` | 次级行动 |

- 圆角统一 `--r-pill`，hover 统一 `translateY(-2px)` + `--sh-2`；字重 `600`
- 一屏内 primary 最多 1 个
- **焦点环是全局的**，不在 `.btn` 上单独写：
  `:focus-visible` → `outline: 3px solid rgb(var(--c-brand-rgb) / .45)` + `outline-offset: 2px`
  ⚠️ 唯一例外是名片的缩放按钮 `.zoom-btn`（自己写了一条 2px 的）—— 那个按钮小，3px 会显得笨重
- ⚠️ 以前这一行写 `--color-primary-dark`，**这个 token 不存在**（语义 token 只有 `--color-brand-deep`）

### 6.2 卡片 `.info-card`

`--color-surface` 底 + **1px 渐变描边** + `--r-md` + `--sh-1`；hover 抬起 `-3px` + `--sh-2`。

- 描边来自 `::before` 那条 **`mask-composite: exclude` 抠环**（`padding: 1px` + 双向 mask 相减），
  **不是**三层边框叠加；hover 时渐变里的白端换成青，描边跟着亮起来
- ⚠️ 规则里那句 `border: 1px solid var(--color-line)` 是给老浏览器的**兜底**，不是主描边
- `::after` 是**跟随鼠标的柔光**（JS 把指针坐标写进卡片的 `--mx` / `--my`，CSS 画一圈径向光），
  平时 `opacity: 0`，hover 变 1
- ⚠️ 以前这一行写的 `--color-border`，**这个 token 不存在**（正确名是 `--color-line`）
- 内边距统一 `--sp-5`（24px）；卡片标题 `.info-card h3` = `--fs-h3` / 字重 `600` / **`--color-brand`**
  ⚠️ 以前这里写「用 `--color-primary-dark`」—— **这个 token 不存在**

### 6.3 标签 `.tag-list li`

底色 **`--color-mint`（浅青）** ＋ 文字 **`--color-brand`（青）** ＋ `--r-pill`；
字号 `--fs-sm`、字重 `500`；父层 `.tag-list` 是 `display:flex; flex-wrap:wrap` ⇒ 横向排布、允许换行。
⚠️ 以前这里写「底色 `--c-100`、文字 `--color-primary-dark`」—— **两个 token 都不存在**（V2.3 收敛成 6 色时去掉了）。

### 6.4 导航 `.nav`

通栏 + 毛玻璃，内层 `.nav-inner` 限 `--page-width`；滚动 `>8px` 加 `--sh-3` 与 `.scrolled`。
**规则：导航项必须覆盖所有带 id 的主区块**，否则滚动高亮会失效。

### 6.5 图片 `.photo-item`

`--r-md` + `--sh-1`；底下一层 `--color-mint`（图没加载出来时不至于空一块）；hover 抬 `-3px`。
`loading="lazy"`、`decoding="async"`，**alt 必须描述画面内容**（不写「图片」这类同义反复）。
瀑布流用的是 **CSS 多列** `.masonry { columns: 3 }`，靠 `break-inside: avoid` 防止一张图被切成两半；
列数 3 → ≤768px 2 列 → ≤480px 1 列。

---

## 7. 响应式断点

| 断点 | 变化 |
| --- | --- |
| ≤ 768px | Bento 网格转单列；瀑布流转 2 列 |
| ≤ 640px | 首屏 padding 收窄；导航横向滚动（隐藏滚动条）；联系区链接换行 |
| ≤ 480px | 瀑布流 1 列 |

---

## 8. 无障碍底线

- 所有交互元素有 `:focus-visible` 可见焦点环
- 纯装饰元素（marquee、跟随光晕）加 `aria-hidden` 且不抢占焦点
- 图片必有 alt；图标 + 文字组合不依赖颜色单独表意
- 尊重 `prefers-reduced-motion: reduce`（关闭全部 animation / transition）
- 正文对比度 ≥ 4.5:1

---

## 9. Do & Don't

| Do | Don't |
| --- | --- |
| 用 `--sp-*` / `--fs-*` token | 直接写 `margin: 13px` |
| 强调色只用在需要被点击/被记住的地方 | 满屏青色、色块堆砌 |
| 新增区块时同步加导航项 | 加了 section 忘记补导航，导致高亮全灭 |
| 改完在 375px 下看一遍 | 只在桌面宽度调参 |

---

## 10. 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-09-17 | v1.0 | 初版：色阶、语义 token、字体尺度、间距/圆角/阴影/动效、组件与无障碍规范；同步落地到 `style.css` |
| 2026-09-17 | v1.1 | **配色收敛 15 色 → 6 色**（ink / muted / brand / mint / bg / surface），其余层次用 `--c-brand-rgb` 配透明度派生，不再手工造色阶；语义 token 重命名；渐变方向改为「往暗里走」，修掉白字压浅青 2.5:1 的对比度问题；新增「3.5 动效：入场颗粒度」——文字走逐行入场，容器走整块淡入 |
| 2026-09-18 | v1.2 | **新增深色主题（§3.1.1）**：应要求「黑底 + 彩色光标效果」，背景改纯黑。⚠️ 关键认识：画布铺黑 = 全站换主题，否则深墨正文看不见。做法是**只在 token 层覆盖**，业务代码未动；开关 = `<html data-theme="dark">`，删掉即回浅色。强调色与派生通道（`--c-brand-rgb`）同步换，实测对比度全部 ≥ AA。另记录 **§3.1.2 效果层不受配色纪律约束**（形状场用 pen 原版彩虹色板）—— 纪律是**被改掉的，不是被忽略的** |
| 2026-09-18 | v1.3 | **新增组件：液态玻璃面板 `.glass-panel`**（联系区，折射背后的形状场）。技术要点记在 `style.css` §22：**祖先层有 `transform`/`filter`/`opacity`/`isolation` 会形成「背景根」，`backdrop-filter` 就只在那棵子树里取样** —— 本站最隐蔽的来源是 `.reveal.visible` 的 `translateY(0)`（**不是 `none`**），所以玻璃不能套 `.reveal`、也不能加 `isolation`。因背景每帧在动，尺寸设上限并留了一行 kill switch 退回纯模糊。带 `@supports` 降级与「减少动效」处理 |
| 2026-09-20 | v1.4 | **组件升级：`.glass-panel` → 液态玻璃名片**（联系区；头像/名字/@handle/数据区/主按钮/缩放）。新增 token **`--font-mono`**（等宽：handle、版本号、数字这类「本来就该对齐」的东西；同样只用系统字体）。⚠️ **两处规范级判断**：① **禁用选字是反模式** —— 源卡写了 `body { user-select: none }`，而名片的存在意义就是让联系方式能被复制，所以全站保持 0 处 `user-select: none`；② **缩放用 `zoom` 而非 `transform: scale()`** —— 放大的意义是让字更清楚，`zoom` 真重排（清晰）、`transform` 只放大像素（糊），且 `transform` 不占布局会盖住页脚。另修正 v1.3 的一条错误结论：**2D `transform` 不构成背景根**（源卡挂着 `transform: scale()` 照样折射），确定构成的是 `opacity < 1` |
